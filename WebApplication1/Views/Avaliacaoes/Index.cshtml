@model IEnumerable<WebApplication1.Models.Avaliacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Classificacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avaliador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Avaliado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Classificacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Avaliador.ID_do_User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Avaliado.ID_do_User)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_da_Avaliacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_da_Avaliacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_da_Avaliacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
