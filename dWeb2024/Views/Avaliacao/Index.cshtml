@model IEnumerable<dWeb2024.Models.Avaliacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id_do_Avaliador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_do_Avaliado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Classificacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comentario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Data)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id_do_Avaliador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_do_Avaliado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Classificacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comentario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_da_Avaliacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_da_Avaliacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_da_Avaliacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
